# gis-mapping

Reports about map rendering, layer management, and GIS-specific UI behavior (MapLibre, Recharts map overlays, etc.).

| Date | Project | Title | What broke / what changed |
|---|---|---|---|
| 2026-09-04 | DSS_Song_Ray | [Station & rain-station markers redesigned as labeled blue dots](SESSION-2026-09-04-DSS_Song_Ray-rain-station-dot-markers.md) | Feature, not a bug — replaced MapLibre's default pin markers for both survey stations and rain stations with a shared `createDotMarker()` helper (custom DOM element: colored circle + always-visible name label), instead of duplicating custom marker code per marker loop. Grepped the whole app for `maplibregl.Marker` first per the user's request to check other map instances before building the helper — found only one file uses it, so no cross-file refactor was needed but the reusable helper was still built for future maps. Both marker types now use the same `MAP_COLORS.hydrology` blue. `tsc -b && vite build` passed on the first attempt, no snags. |
| 2026-08-26 | DSS_Song_Ray | [Import watershed boundary + rain stations, expose as GIS API](SESSION-2026-08-26-DSS_Song_Ray-import-watershed-rain-stations-gis-api.md) | Feature, not a bug — inspected a new GIS data folder (no local GDAL, so an Explore subagent parsed .shp/.dbf/.prj binary formats directly), found 1 watershed polygon, 1 river centerline, and 14 rain gauges in EPSG:32648 (distinct from the project's legacy custom-TM BDHC CRS); generalized `shp-to-geojson.ts`'s `convertToGeoJson()` to accept optional sourceDir/CRS/encoding overrides instead of duplicating it, added 2 new tables + 2 ingest scripts + 2 JWT-guarded `/gis/*` endpoints + real FE map layer/markers replacing a previously inert "Mưa" placeholder toggle. Migration and ingest were not run against a live DB in this session — flagged as an explicit gap. |
| 2026-08-25 | DSS_Song_Ray | [FE simulation downstream impact panel (P9-07)](SESSION-2026-08-25-DSS_Song_Ray-downstream-impact-panel.md) | Feature, not a bug — built a MapLibre affected-area/depth panel + step scrubber + damage stats, following P9-06's schema-gap empty-state pattern since `simulation_results` has no inundation polygon/damage field and Muskingum routing only propagates discharge. Hit `TS2503: Cannot find namespace 'GeoJSON'` (no `@types/geojson` in this project) — fixed by typing the field as `maplibregl.GeoJSONSourceSpecification["data"]` instead. Also had to fetch+merge the primary checkout's `main` by local path first, since the worktree branch and even `origin/main` were 5 commits stale and missing P9-04/05/06 dependency code. |
| 2026-08-25 | DSS_Song_Ray | [FE comparison map — side-by-side / overlay toggle (P10-03)](SESSION-2026-08-25-DSS_Song_Ray-comparison-map-side-by-side.md) | Feature, not a bug — built a two-MapLibre-instance synced side-by-side/overlay flood comparison view (camera sync via `move` listener + `syncingRef` feedback-loop guard), reusing the P7-02 basemap pattern. No real per-scenario flood-extent data exists yet (DEM/raster source blocked at P11-D1, and the BE's comparison service never persists an area delta), so each pane shows an honest "chưa có dữ liệu" empty-state banner instead of fabricated flood polygons, matching this repo's established empty-state convention. Also had to `git merge main` first to pull in two already-merged dependency tasks the worktree branch was missing. |
| 2026-08-24 | EWATER | [Add Esri Topo basemap to GIS map](SESSION-2026-08-24-EWATER-esri-topo-basemap.md) | Feature, not a bug — user pasted a Leaflet `L.tileLayer` snippet for an Esri topo service; translated to this project's MapLibre GL JS stack (`addSource`/`addLayer` raster pair) and wired as a third basemap option following the existing OSM/Satellite pattern across 7 files (config JSON+SQL, 3 TS union types, panel list, CSS swatch, i18n keys). SQL update to the live `app_config` DB row was left unapplied as a follow-up — flagged as an inferred (not runtime-verified) assumption about the runtime config-loading path. |
| 2026-08-18 | EWATER | [Responsive height clamp squeezed map + detail-card below 1200px on `/gis-map`](SESSION-2026-08-18-EWATER-gis-map-responsive-height-clamp-fix.md) | `.gis-page-grid`'s narrow-breakpoint overrides hard-capped its single flexible row (`clamp(360px,48vh,560px)` / `clamp(280px,45vh,420px)` + `height: auto`) instead of filling the viewport, starving both the map canvas and `.gis-side-column` (gate/rain-station detail-card) — the clamp pattern was inferred to be a stale copy-paste from `.mon-page-grid`'s real 5-track-collapse fix, which doesn't apply here (only 1 flexible track); replaced with `minmax(floor, 1fr)` + `height: calc(100vh - 130px)` matching the desktop rule. Not yet visually verified in a browser. |(SESSION-2026-08-17-EWATER-flood-raster-image-source-pivot.md) | Continuation of the crossfade fix below — 2 more real bugs fixed (a stale `sourcedata` listener race, and MapLibre's default `raster-fade-duration` stacking on top of the hand-rolled crossfade) still left similar flicker, so the session stepped back to an architecture decision: replaced the `raster` tile source entirely with an `image` source + `updateImage()` (a true atomic swap), served from a small per-step preview PNG (~1500px, generated at the app's actual display zoom) instead of the original full-resolution COG |
| 2026-08-17 | EWATER | [Flood raster crossfade fix — "giật" on timeline step change](SESSION-2026-08-17-EWATER-flood-raster-crossfade-fix.md) | User suspected titiler's server-side colormap step caused a stutter; measured and disproved that, then found the real cause (step-change effect deleted the visible layer before its replacement existed) and fixed it with a 2-slot ping-pong crossfade gated on MapLibre's `sourcedata`/`isSourceLoaded` event |
| 2026-08-17 | EWATER | [Checkbox to toggle river/canal layer on `/gis-map`](SESSION-2026-08-17-EWATER-gis-river-layer-toggle.md) | Missing feature, not a bug — added a `showRivers` toggle for the water polygon + centreline, using MapLibre's `visibility` layout property instead of `removeLayer` because the centreline is the `beforeId` stacking anchor for the flood/DEM rasters |
