# frontend-ui — index

Reports about React/frontend UI behavior: component state, effects, async loading races, rendering.

| Date | Project | Title | What broke / what changed |
|---|---|---|---|
| 2026-09-04 | DSS_Song_Ray | [KpiCard reworked into a 3-row centered layout](SESSION-2026-09-04-DSS_Song_Ray-dashboard-kpicard-3x2-layout.md) | Small single-commit CSS-only follow-up to a same-day centering fix — user specified directly (as CSS pseudo-code, not a question) reworking `.kpi-card`'s grid-template-areas from icon-spans-two-rows-beside-title/value into a 3-row layout (title full-width row, icon+value sharing the middle row, caption full-width row), with `justify-self: center` on every grid child. One addition beyond the literal spec: `.kpi-card-caption` also needed `justify-items: center` since it's a nested grid container itself (holds up to 2 lines) — `justify-self` only centers the box, not its own children. Icon column width also went from `auto` to a hardcoded `38px`, untied to the `--icon-badge-size-md` (30px) token — flagged as a gotcha. Build passed clean first try. |
| 2026-09-04 | DSS_Song_Ray | [Hide two lowest-priority rain KPI cards at the lg breakpoint](SESSION-2026-09-04-DSS_Song_Ray-dashboard-kpi-hide-rain-lg-tier.md) | 3rd session on the same 1200-1535px ragged 6+2 KPI row bug — after two prior redesigns (container queries + grouping, then an asymmetric strip layout) were both reverted for being more than the user wanted, this session used the smallest possible fix: wrapped the two lowest-priority rain cards ("Mưa 24 giờ qua", "Mưa dự báo 72 giờ tới") in a `.dashboard-kpi-item--hide-lg` div and hid them only in that one tier via `@media (--bp-lg-up) and (--bp-xl-down)`, so the remaining 6 fill the existing 6-col grid exactly; all 8 reappear at xl-up (8 cols). No shared-component changes, build passed first try. |
| 2026-09-04 | DSS_Song_Ray | [Dashboard KPI row redesign — single asymmetric summary strip](SESSION-2026-09-04-DSS_Song_Ray-dashboard-kpi-strip-layout.md) | Follow-up to the 09-03 KPI grouping session — user reviewed the equal-width 4-card grid and flagged it as wrong since the four groups carry unequal content (1/1/2/4 metrics). Redesigned into one strip container (shared chrome moved off the individual cards onto `.dashboard-kpi-row`) with asymmetric `0.8fr/0.8fr/1.2fr/1.6fr` columns at the widest tier, `nth-child`-scoped divider borders per container-width tier (blanket `:not(:last-child)` rejected — breaks once the grid wraps rows), and value+caption merged onto one row via `grid-auto-flow: column` (not Flexbox, per project convention). Sparkline/progress-bar addition floated by the user was explicitly skipped — no real water-level/storage history endpoint exists yet, confirmed via grep + `WaterLevelChartCard`'s own demo-series comment, so it would have needed fake data. One `!important`-vs-specificity snag caught in self-review before any build error. |
| 2026-09-03 | DSS_Song_Ray | [Dashboard KPI row responsive redesign — container queries + card grouping](SESSION-2026-09-03-DSS_Song_Ray-dashboard-kpi-responsive-redesign.md) | Not a bug — planned 3-phase feature. Root cause of the reported 1200-1535px ragged 6+2 KPI layout: sidebar only auto-collapses below 1200px, so viewport width alone can't tell the row its real available width once the 160px sidebar is expanded. Fixed by (1) a compact-density tier, (2) moving the KPI row to `@container` queries scoped to a new wrapper instead of `@media`, deliberately leaving `.dashboard-main-grid` on viewport queries to avoid reintroducing a previously-fixed `grid-template-areas` desync bug, and (3) grouping 8 single-metric cards into 4 business-meaning cards via a new `KpiGroupCard` component, replacing a fragile `title === "Dung tích hồ"` string-check with an explicit `precision` prop. |
| 2026-09-03 | DSS_Song_Ray | [Dead-code removal, SimulationPage hook extraction, OperationLogPage layout fix](SESSION-2026-09-03-DSS_Song_Ray-simulation-workbench-hook-and-cleanup.md) | Three cleanups: deleted unused `FloodMapPage.tsx` after grep confirmed zero live references; extracted `SimulationPage.tsx`'s dense inline state/effects/handlers (9 `useState` on one line) into a new `useSimulationWorkbench()` hook plus `workbench-validation.ts`, verified via `pnpm build` before/after; removed `OperationLogPage.css`'s negative-margin/height-calc hack for escaping shell padding, discovered to be dead-code double-compensation against an already-existing but never-adopted `page-root--flush` mechanism in `AppShell.tsx` — deleted the flush mechanism entirely per user's choice to standardize on default page padding, following the project's ask-before-CSS-fix rule. |
| 2026-08-28 | DSS_Song_Ray | [Rainfall chart multi-station selection + simulation timeline responsive slider](SESSION-2026-08-28-DSS_Song_Ray-forecast-multi-station-and-timeline-slider.md) | Two changes: (1) rainfall forecast chart's single-station tab row became a `StationMultiSelect` checkbox dropdown with a composite `${stationId}__${sourceCode}` chart key to avoid data collisions; the shared `stationId` state also fed a summary card that needs exactly one active station, so `selectedStationIds[0]` was derived rather than pushing the array everywhere — a deliberate deviation from the literal instruction, explained to the user. (2) GIS simulation timeline's overlapping labels (screenshot: `"15:0529-08-0"`) traced to a hardcoded `MAX_LABELS = 10` independent of track width; replaced the 72-dot row with a native range input + `ResizeObserver`-driven dynamic label count; an invalid `clamp()`/CSS-var edge-guard attempt was caught in self-review (not by a build error) and simplified to plain `translateX(-50%)`. |
| 2026-08-28 | DSS_Song_Ray | [Comparison page decision rail — 4-state recommendation panel](SESSION-2026-08-28-DSS_Song_Ray-comparison-decision-rail.md) | Feature/bug fix — the old decision rail always rendered a green "Khuyến nghị" trophy badge regardless of the backend's actual 4-state `ComparisonRecommendation.status` (recommended/priority-review/needs-expert/not-recommendable), misleading for flood-operations decisions; rewrote `DSSRecommendationPanel.tsx` to render state-driven label/tone/icon plus ranking chips, compliance count, and separated strengths/violations, all sourced straight from the backend with zero client-side recomputation. Also replaced `ForecastConfidenceCard`'s 180x100 SVG semicircular gauge with a compact `role="meter"` bar keyed off the backend's `confidenceBand`, removing a duplicate client-side percent-threshold function. No bugs hit — `tsc --noEmit` passed clean on first pass; one deliberate scope gap recorded (`/simulation` doesn't yet consume the `focusScenarioId` router state this panel now sends). |
| 2026-08-26 | DSS_Song_Ray | [Status light card redesign — layered LEDs + CardHeader tooltip](SESSION-2026-08-26-DSS_Song_Ray-status-light-card-redesign.md) | Feature, not a bug — rebuilt the aggregate status light card (dark navy theme, 4-layer LED per light: housing/halo/core/highlight, glow-pulse animation on the active one) and added an optional `tooltip` prop to the shared `CardHeader` component instead of forking a one-off header, keeping every other card's markup unchanged. Main snag: first CSS draft used `display: flex` for the info-icon row and status banner, violating this repo's binding CSS-Grid-only convention — caught by re-reading loaded memory mid-session, fixed to `display: grid; grid-auto-flow: column`, verified with a `grep` for zero remaining `display: flex`. Done in a worktree (`worktree-status-light-card-redo`) then merged `--no-ff` into `main`. |
| 2026-08-25 | DSS_Song_Ray | [P9-08 Simulation page layout parity against template](SESSION-2026-08-25-DSS_Song_Ray-p9-08-simulation-layout-parity.md) | Not a bug — task assumed P9-04..P9-07 were already merged and the real `SimulationPage.tsx` existed to lay out, but the worktree branch was 24 commits behind `main` and still had the pre-P9-04 stub; caught via `git log --all --oneline | grep P9-0` and fixed with `git merge main --no-edit` before any layout work. Then converted the last leftover Flexbox in `SimulationPage.css` to CSS Grid (project convention) and grouped scenario-selector + run-controls into a new `.simulation-input-row` 2-col grid to mirror the template's top-row card grouping; full visual parity is blocked on real data (map card, KPI charts) per the no-synthetic-data rule, so scope stayed CSS-only. |
| 2026-08-25 | DSS_Song_Ray | [P10-05 FE comparison summary card — honest delta recommendation](SESSION-2026-08-25-DSS_Song_Ray-comparison-summary-card.md) | Feature, not a bug — task asked for a "which scenario is more protective" recommendation card, but the codebase's own P10-02 precedent already established no domain rule exists for that (flood-control vs. drought priority never supplied); built `ComparisonSummaryCard.tsx` as a factual, non-directional delta summary (plain-language magnitude/direction, two size-only "significant" thresholds) plus an explicit disclaimer instead of fabricating a verdict. Same recurring snag as sibling sessions today: the worktree branch was stale (last commit P9-02) while local `main` had P9-07/P10-01..04/P12-05 merged — confirmed via `git log`/`git branch -vv` and fixed with `git rebase main`. |
| 2026-08-25 | DSS_Song_Ray | [P12-05 FE alerts page with live feed and acknowledge action](SESSION-2026-08-25-DSS_Song_Ray-alerts-page-live-feed.md) | Feature, not a bug — built the alerts page (color-coded severity via `StatusBadge` reuse, time-since via a new `timeSince()` helper, acknowledge via the existing `alertsService.ts`, dismiss via a `localStorage` id-set since no server-side "dismissed" flag exists). Main snag: this worktree's branch was several sessions behind `main` (missing P12-04/P4-04's commits entirely despite `tasks/INDEX.md` marking them done) — `git fetch origin`/`localmain` both failed, but `remotes/localmain/main` already had the refs cached, so `git merge localmain/main` pulled them in directly. Also reused a same-day P9-04 finding that no i18n system exists in this repo, so all strings stayed hardcoded Vietnamese. |
| 2026-08-18 | EWATER | [Chart cards collapse to a sliver below 1200px on `/forecast/overview`](SESSION-2026-08-18-EWATER-forecast-overview-responsive-card-clipping-fix.md) | `.fc-top-grid`'s `≤1200px` override switched to a stacked single column but never relaxed the container's fixed `height: clamp(640px, ..., 1300px)`, clipping content; separately the rain/water-level `.dash-chart-card`'s `flex:1; height:auto` canvas had no real ancestor height for Recharts `ResponsiveContainer height="100%"` to resolve against and collapsed to ~0 (only the top axis label visible, matching the screenshot) — fixed with `height: auto` on the grid plus explicit `220px` canvas heights, reusing the pattern from `.dash-side2`/`.dash-side3`. Not yet visually verified. |
| 2026-08-18 | EWATER | [onAuthStateChange reentrancy deadlock on first load](SESSION-2026-08-18-EWATER-auth-onauthstatechange-deadlock.md) | First-load spinner deadlocked every time: `loadProfile()` was `await`ed directly inside `onAuthStateChange`, blocking on GoTrueClient's internal lock (confirmed via absent `/rest/v1/profiles` Resource Timing entry); fixed by deferring the call via `setTimeout(0)` so it runs after GoTrueClient releases the lock; includes a false lead (HMR-remount-confounded repro) ruled out along the way |
| 2026-08-18 | EWATER | [Instrument auth bootstrap phase for intermittent first-load hang](SESSION-2026-08-18-EWATER-auth-bootstrap-logging.md) | First-load spinner sometimes hangs forever despite prior fixes (Supabase client singleton, redundant getSession() removal, 20s data-load timeout); found the auth-bootstrap phase had zero success-path logging, added console instrumentation to `AuthContext.tsx`/`App.tsx` to catch the next reproduction — root cause still unidentified, this is diagnostic tooling only |
